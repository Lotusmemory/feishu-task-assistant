# 2026-07-15 修改任务问题排查交接

## 文档目的

用于在新任务中快速恢复本次排查上下文，避免重复解释背景。

生成时间：2026-07-15 14:46:05 +0800

## 用户原始诉求

用户希望在私聊客服时：

- 当发送“修改任务”但**没有指定具体任务名**时，不要落到知识库兜底回复。
- 期望先搜索自己状态为`未开始`、`进行中`、`阻塞中`、`延期中的任务`。
- 然后生成任务选择卡片。
- 用户点选任务后，再进入现有“修改任务”编辑卡片。

用户随后反馈：

- 发送“修改任务”后，实际仍收到知识库兜底文案：
  `当前知识库暂无此信息。我已将问题记入待补清单；如需人工回访，请发送“需要回访：你的问题”。`

## 本次已完成改动

### 1. 任务意图解析

已修改 [src/task-intent.js](/Users/tjc/Ai%20/kefu/src/task-intent.js:74)：

- 现在 `修改任务` / `帮我修改任务` / `编辑任务` 这类**未带任务名**的文本，
  会被解析为：

```json
{
  "operation": "edit_task_form",
  "selector": { "ownerOpenId": "me" },
  "fields": {}
}
```

### 2. 服务层返回“可编辑任务选择卡”

已修改 [src/task-service.js](/Users/tjc/Ai%20/kefu/src/task-service.js:10) 与 [src/task-service.js](/Users/tjc/Ai%20/kefu/src/task-service.js:83)：

- 对未指定任务名的编辑请求，只筛选当前用户任务。
- 只返回状态为：
  - `未开始`
  - `进行中`
  - `阻塞中`
  - `已延期`
- 返回类型为 `edit_task_picker`，供消息层渲染任务选择卡。

说明：

- 用户口头说“延期中”，项目现有状态值使用的是 `已延期`，本次沿用现有枚举，没有新增第二套状态名。

### 3. 卡片层新增“选择要修改的任务”

已修改 [src/feishu-messenger.js](/Users/tjc/Ai%20/kefu/src/feishu-messenger.js:305)：

- 新增 `buildTaskEditPickerCard(tasks)`。
- 每条任务带一个 `修改` 按钮。
- 按钮回调值为：

```json
{ "action": "edit_task", "taskId": "..." }
```

- 同时新增 `parseTaskEditSelectionAction(event)` 解析该回调。

### 4. 选择任务后进入现有编辑卡

已修改 [src/message-handler.js](/Users/tjc/Ai%20/kefu/src/message-handler.js:85) 和 [src/message-handler.js](/Users/tjc/Ai%20/kefu/src/message-handler.js:164)：

- `edit_task_picker` 会回复为任务选择卡。
- 点选 `修改` 后，会重新调用任务服务查询该任务。
- 再进入原来的 `buildTaskEditCard(task)` 编辑卡流程。
- 已在回调时附带 `ownerOpenId` 做负责人校验，避免仅凭 `recordId` 打开别人的任务。

## 已做验证

### 本地逻辑验证

直接在仓库里执行过本地验证：

- `修改任务` 能被解析成 `edit_task_form + ownerOpenId: me`
- 消息处理器会返回“选择要修改的任务”卡，而不是 RAG 文本兜底

本地复现实验结论：

- 当前代码路径是正确的
- 问题不在新增逻辑本身

### 测试验证

已通过：

- `node --test test/task-intent.test.js test/task-service.test.js test/feishu-messenger.test.js test/message-handler.test.js`
- `npm test`

结果：

- 全量 `169` 个测试通过

## 本次定位到的真实问题

问题核心不是代码未实现，而是：

- 线上正在运行的客服进程还是**旧版本内存态**
- 因此用户发送“修改任务”时，仍走到了旧逻辑，最终落到知识库兜底

排查证据：

- 机器上当时存在运行中的 `node src/index.js`
- 工作目录确认是本仓库：`/Users/tjc/Ai /kefu`
- 但该进程未重启到最新代码

## 本次已执行的运维操作

已执行：

1. 停掉旧的 Node 进程
2. 重启 `node src/index.js`
3. 收口为单实例，避免重复消费飞书事件

当前确认状态：

- 运行实例已更新为新进程
- 当时最终保留的单实例 PID 为 `45364`

备注：

- PID 只对当时排查时刻有效，后续重启后可能变化

## 对当前状态的判断

按照代码和进程状态，发送：

- `修改任务`
- `帮我修改任务`

现在理论上应该返回“选择要修改的任务”卡片，而不是知识库兜底文案。

## 如果用户再次反馈“还是不行”，下一步怎么查

下一步不要再猜，直接抓真实事件内容。

优先排查：

1. 飞书实际传进来的 `message.content` 是否真的是 `"修改任务"`
2. 是否存在隐藏字符、换行、富文本、前缀、引用消息内容污染
3. `extractPrompt(message)` 提取后的文本到底是什么
4. 是否有另一个未发现的旧实例还在消费事件
5. 是否飞书客户端发出的并不是 `text` 消息类型

建议在新任务里直接做：

- 给 `createMessageHandler` 临时加一条安全日志：
  - 原始 `message.content`
  - `message.message_type`
  - `message.chat_type`
  - `extractPrompt(message)` 的结果
- 然后让用户重新发送一次“修改任务”
- 用真实输入继续定位

## 新任务建议提示词

可直接把下面这段贴给新任务：

```md
请基于 context/2026-07-15-修改任务问题排查交接.md 继续排查。

已知：
- “修改任务”无任务名时，代码理论上应走任务选择卡，而不是知识库兜底
- 本地逻辑验证通过
- 全量测试通过
- 之前发现过运行中的旧 Node 进程未重启，已处理

如果问题再次出现，请优先抓飞书真实事件 payload 和 extractPrompt 结果，不要先猜业务逻辑。
```

## 相关文件

- [src/task-intent.js](/Users/tjc/Ai%20/kefu/src/task-intent.js)
- [src/task-service.js](/Users/tjc/Ai%20/kefu/src/task-service.js)
- [src/feishu-messenger.js](/Users/tjc/Ai%20/kefu/src/feishu-messenger.js)
- [src/message-handler.js](/Users/tjc/Ai%20/kefu/src/message-handler.js)
- [test/task-intent.test.js](/Users/tjc/Ai%20/kefu/test/task-intent.test.js)
- [test/task-service.test.js](/Users/tjc/Ai%20/kefu/test/task-service.test.js)
- [test/feishu-messenger.test.js](/Users/tjc/Ai%20/kefu/test/feishu-messenger.test.js)
- [test/message-handler.test.js](/Users/tjc/Ai%20/kefu/test/message-handler.test.js)
